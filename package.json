{
  "name": "cc-suite",
  "version": "1.2.0",
  "private": true,
  "type": "module",
  "description": "One plugin to bridge Claude Code and Codex CLI \u2014 shared AGENTS.md, mirrored hooks and MCP servers, bidirectional delegation \u2014 plus delegation to Antigravity CLI (agy)",
  "license": "ISC",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
