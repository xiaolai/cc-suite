{
  "name": "cc-suite",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "description": "Bridge Claude Code, Codex CLI, Antigravity CLI, Grok Build, and bounded Qwen review with shared project state and supervised delegation",
  "license": "ISC",
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
